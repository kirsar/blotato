# 7. Security & Multi-tenancy — implementation plan

Decisions and rationale: see [0.foundational-research.md](./0.foundational-research.md) §7.

*Not yet detailed.* Expected contents once written: the `ApiKeyGuard` implementation
(see [1.overall-architecture.md](./1.overall-architecture.md)'s root-module entry),
how `userId` scoping is enforced in repository queries, and which parts (if any) of
row-level security get built vs. documented per the Scope guardrail. Schema support
(`User`, `ApiKey`) already sketched in [5.storage.md](./5.storage.md).

Note the tenant is `User`, not a `Workspace` layer above it — §7.0 records why that was
dropped. `Comment` and `Post` carry `userId` denormalized so an RLS policy on the
largest tables doesn't join through `SocialAccount` on every row check.
